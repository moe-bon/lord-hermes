FROM python:3.13-slim
RUN apt-get update && apt-get install -y --no-install-recommends curl && rm -rf /var/lib/apt/lists/*
WORKDIR /workspace
COPY python/apexquant-backup-recovery-core ./python/apexquant-backup-recovery-core
RUN pip install --no-cache-dir ./python/apexquant-backup-recovery-core
EXPOSE 8093
CMD ["uvicorn", "apexquant_backup_recovery_core.app:app", "--host", "0.0.0.0", "--port", "8093"]
